{
  "name": "performance-decorators",
  "version": "3.0.4",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "clean": "pnpm -r run clean",
    "test": "jest --config=jest.config.base.ts --passWithNoTests",
    "test:node": "jest --config=jest.config.node.ts --passWithNoTests",
    "test:browser": "jest --config=jest.config.browser.ts --passWithNoTests",
    "publish-all": "pnpm -r publish --filter './packages/{node,browser}' --access public"
  },
  "devDependencies": {
    "@jest/types": "^29.6.3",
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "ts-jest": "^29.3.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
